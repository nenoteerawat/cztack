name: Greet Everyone

on: [push]

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    steps:
      - name: pwd
        run: pwd
      - name: path
        run: echo $PATH
      - name: ls
        run: ls
      - name: brew versoin
        run: brew --version
      - name: brew bundle install
        run: brew bundle install
